{
  "name": "actual-egg-timer",
  "version": "0.2.0",
  "description": "Actual Egg Timer: a boiled-egg timer that computes the time from transient conduction and denaturation kinetics",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "type": "module",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "tsc && node --test dist/test/*.test.js",
    "validate": "tsc && node dist/tools/validate.js",
    "fixtures": "tsc && node dist/tools/fixtures.js",
    "identifiability": "tsc && node dist/tools/identifiability.js",
    "conformance": "npm run fixtures && cd ios/EggTimerCore && swift test",
    "serve": "python3 -m http.server 8080",
    "build:site": "tsc -p tsconfig.site.json && cp index.html styles.css site.webmanifest _site/ && cp -R assets _site/assets",
    "serve:site": "python3 -m http.server 8080 --directory _site"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^5.6.0"
  }
}
